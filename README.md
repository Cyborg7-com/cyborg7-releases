# Cyborg7 Releases

Public distribution of Cyborg7 desktop builds (DMG/ZIP) and auto-update metadata (`latest-mac.yml`).

The application source lives in the private `cyborg7-core-rewrite` repo. Releases are published here automatically by CI so `electron-updater` can read update metadata without authentication.
